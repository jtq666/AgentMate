# AgentMate 使用、演示与测试指南

本文适用于当前 AgentMate v1.0：AI Agent 专题研学与计算机保研面试训练平台。

## 新手第一次使用：只做这四步

1. 打开“研学工作台”，点击“一键填入推荐配置”。
2. 点击“开始生成研学内容”，等待页面自动完成四个准备阶段。
3. 阅读学习讲解后，点击“下一步：完成模拟面试”，回答 5 道题并提交。
4. 前往“学习报告”查看掌握度、薄弱点，并下载 Markdown 报告。

首次体验不需要先上传资料，也不建议开启“额外补充论文”。侧边栏的“显示新手引导”可随时打开或关闭；答辩时可开启“答辩演示模式”查看 Agent 分工、耗时和技术证据。

## 1. 环境准备

推荐使用 Python 3.12。在项目根目录安装依赖：

```bash
pip install -r requirements.txt
```

复制 `.env.example` 为 `.env`，至少配置一个 OpenAI 兼容模型接口：

```env
OPENAI_API_KEY=你的密钥
OPENAI_BASE_URL=https://你的接口地址/v1
OPENAI_MODEL=模型名称
```

可选运行参数：

```env
AGENTMATE_STUDY_DB_PATH=agentmate/data/agentmate.db
AGENTMATE_STAGE_TIMEOUT_SECONDS=90
AGENTMATE_STAGE_MAX_RETRIES=1
AGENTMATE_API_URL=http://127.0.0.1:8000
```

## 2. 启动和停止服务

在项目根目录打开两个终端。

终端一启动后端：

```bash
python run_server.py
```

终端二启动前端：

```bash
streamlit run agentmate/frontend/app.py --server.port 8501
```

Windows 也可以分别双击：

- `start_backend.bat`
- `start_frontend.bat`

访问地址：

- 前端：http://127.0.0.1:8501
- 后端健康检查：http://127.0.0.1:8000/health
- API 文档：http://127.0.0.1:8000/docs

停止服务时，在两个终端分别按 `Ctrl+C`。不要直接删除数据库或 ChromaDB 目录。

## 3. 推荐答辩演示流程

建议使用 `ReAct` 专题完成一次 5～8 分钟演示。

### 第一步：创建研学任务

1. 打开“研学工作台”。
2. 专题选择 `ReAct`；如需自定义，先在专题下拉框选择“自定义主题”，再输入与 AI Agent 直接相关的主题。
3. 目标选择 `准备面试`，难度选择 `进阶`。
4. 根据网络情况决定是否勾选“补充检索最多 3 篇论文”。
5. 点击“开始生成研学内容”。

创建任务时可在“模拟面试题型”中组合选择：

- 综合问答
- 概念辨析
- 系统设计
- 项目深挖
- 论文追问

系统仍统一生成 5 道递进问题，但只会使用选中的题型。

按钮点击后应立即出现成功提示，后端返回任务 ID，状态从 `queued` 进入 `running`。创建接口不会同步等待完整模型链路。

#### 自定义主题检查

AgentMate 当前不是通用计算机课程平台，自定义主题只支持 AI Agent 范围：

- `Agent 长期记忆评估` 且资料充足：检查通过并创建任务。
- `MCP Agent 工具协议` 但资料不足：不创建任务，显示“前往资料库上传相关资料”。
- `Transformer`：明确提示不在支持范围，并推荐六个内置专题。

主题范围和资料覆盖度由后端再次校验，因此失败的主题不会写入任务历史或进入后台队列。上传 Agent 细分主题资料后，点击“返回工作台重新检查”即可继续创建。

### 第二步：观察固定多 Agent 流水线

页面会自动轮询以下四个阶段：

```text
Research → Teaching → Interview → Supervisor
```

- Research：执行 ChromaDB + BM25 + RRF 混合检索，并分配 `[S1]` 引用。
- Teaching：生成学习地图、核心概念、示例和常见误区。
- Interview：生成 5 道递进面试题，内部评分规则不会提前显示。
- Supervisor：校验结构化产物和引用，生成初始报告。

如果任务失败或因重启中断，页面会显示具体原因和“从失败阶段重试”按钮。

### 第三步：阅读与任务内追问

任务生成完成后：

1. 展开核心概念阅读结构化讲解。
2. 展开“资料来源与引用预览”，确认 `[Sx]` 能对应真实来源。
3. 在“AI 研学导师”输入问题，或直接点击概念下方的“没听懂，换个例子”“生成面试表达”。

追问只使用当前任务资料，回答中的引用必须出现在任务来源列表中。资料不足时系统应明确说明，而不是编造来源。

“导师答疑”支持多个独立会话。可以通过“管理会话”创建、重命名或删除会话，不同会话的上下文不会混在一起。

### 任务与资料管理

- 工作台的“管理当前任务”支持重命名、归档、恢复和永久删除。
- “显示归档”用于查看已归档任务。
- 资料库的“管理资料”支持名称筛选、分页、查看内容、重命名和确认删除。
- 永久删除任务会级联删除对应评测和对话，操作前必须勾选确认。

### 第四步：完成实践评测

1. 切换到“实践评测”。
2. 选择刚完成的研学任务。
3. 每次回答一道题，使用“上一题”“下一题”切换，草稿会保留在当前会话。
4. 到达第 5 题后，点击“提交 5 题并开始评分”。

任意一题留空时，按钮会显示“请完成全部 5 道题后再提交”，后端不会创建评测。全部填写后，Evaluation Agent 在后台逐题评分并返回：

- 每题得分；
- 命中知识点；
- 遗漏点和错误概念；
- 针对性复习建议；
- 基于测评证据的专题掌握度。

每道题的反馈下方可以点击“让研学导师讲解本题遗漏”，系统会携带遗漏点和错误概念回到专题导师答疑。学习报告中的“根据薄弱点创建复习任务”会把这些弱点传入下一轮 Research、Teaching 和 Interview，而不是简单重复原任务。

### 论文筛选与收藏

“资料库 → 查找论文”支持搜索前设置年份范围、最低引用数、来源和返回数量；搜索后可按标题、作者、会议、来源、有无摘要继续筛选，并使用“近三年、高引用、重要会议、有 PDF、仅收藏”快捷条件。论文卡片会显示推荐原因，可分别收藏或加入资料库。

项目已经取消“掌握、困难、跳过”等手动按钮，掌握度只能由测评产生。

### 第五步：导出学习报告

1. 切换到“学习报告”。
2. 查看各专题掌握度和优先复习项。
3. 选择历史任务。
4. 点击“下载 UTF-8 Markdown 报告”。

报告包括学习地图、核心讲解、测评得分、薄弱点、建议和可追溯来源。

## 4. 资料库使用

### 导入文本

在“上传资料”中填写标题和正文，点击“导入文本”。成功后会提示新增知识块数量。

### 上传文件

支持 Markdown、TXT、PDF、DOCX、JSON 和 CSV，单文件最大 10 MB。选择文件后点击“导入文件”。扫描版 PDF 没有文本层时可能无法抽取内容。

### 混合检索

输入检索问题并点击“执行 ChromaDB + BM25 + RRF 检索”。结果会显示：

- RRF 分数；
- 来源类型；
- `document_id`；
- 内容预览。

### 论文检索和入库

1. 输入英文或中文主题。
2. 点击“搜索论文”。
3. 查看标题、摘要、年份、引用数和来源链接。
4. 勾选需要的论文，点击“导入选中论文”。

Semantic Scholar 或 arXiv 失败不会阻塞本地研学任务。系统会记录警告并使用本地资料继续。

### 删除资料

“资料管理”中的“删除”按钮会立即删除对应知识块。删除前应核对来源名称；内置课程资料删除后，需要重启后端才会重新导入。

## 5. 按钮预期响应表

| 页面 | 按钮 | 正常响应 |
|---|---|---|
| 研学工作台 | 检查自定义主题 | 支持时创建任务；资料不足时显示上传入口；越界时明确拒绝 |
| 研学工作台 | 创建研学任务 | 显示成功提示，新增任务并进入 queued/running |
| 资料库 | 返回工作台重新检查 | 保留自定义主题并返回创建区 |
| 研学工作台 | 从失败阶段重试 | 从 failed/interrupted 阶段重新排队 |
| 资料库 | 导入文本 | 显示导入知识块数量 |
| 资料库 | 导入文件 | 显示文件名和导入数量 |
| 资料库 | 执行混合检索 | 展示检索结果和 document_id |
| 资料库 | 搜索论文 | 展示最多指定数量的论文 |
| 资料库 | 导入选中论文 | 选中论文写入资料库 |
| 资料库 | 删除 | 对应 document_id 消失 |
| 实践评测 | 提交全部回答 | 完整答案进入异步评分；空答案明确报错 |
| 学习报告 | 下载报告 | 下载 UTF-8 `.md` 文件 |

## 6. 页面刷新和异常恢复

- **刷新页面**：任务 ID 保存在当前浏览器会话中，前端会重新从后端读取任务状态。
- **后端重启**：运行中任务会标记为 `interrupted`，已完成产物不会丢失。
- **论文接口失败**：Research 显示警告，任务回退本地资料。
- **核心阶段失败**：默认超时 90 秒并自动重试一次；仍失败时显示失败原因。
- **按钮点击后长时间无变化**：先检查页面顶部或侧栏是否显示“服务运行中”，再访问 `/health`。

## 7. “按钮没反应”排查

按以下顺序检查：

1. 访问 http://127.0.0.1:8000/health，确认返回 `status: ok`。
2. 确认前端使用的是 http://127.0.0.1:8501，而不是旧浏览器标签页。
3. 强制刷新浏览器：Windows 使用 `Ctrl+F5`。
4. 查看后端终端是否出现 4xx、5xx、模型超时或限流信息。
5. 查看前端终端是否出现 Streamlit 异常。
6. 确认表单必填项完整，例如自定义主题不少于 2 个字符、评测必须回答 5 题。
7. 如果任务状态是 `queued`，说明前面已有任务正在运行，并非按钮失效。
8. 若仍无法恢复，停止两个服务后重新启动；中断任务可点击重试。

Windows 可检查端口：

```powershell
Get-NetTCPConnection -State Listen |
  Where-Object { $_.LocalPort -in 8000,8501 } |
  Select-Object LocalPort,OwningProcess
```

## 8. 工程测试

安装测试依赖并运行：

```bash
pip install -r requirements-dev.txt
python -m compileall agentmate run_server.py
pytest --cov=agentmate.study --cov-report=term-missing --cov-fail-under=80
ruff check agentmate tests
```

当前验收结果：

- pytest：9 项通过；
- 核心研学模块覆盖率：80.47%；
- Ruff：通过；
- 四页导航、任务创建、资料导入/检索/删除、论文搜索、完整评测和报告下载已完成按钮级联调。

## 9. 24 题对比实验

只校验题集：

```bash
python -m agentmate.benchmarks.run_benchmark
```

执行真实模型实验：

```bash
python -m agentmate.benchmarks.run_benchmark --live
```

完整实验包含 24 题 × 4 配置，共 96 个样本。结果保存在：

- `docs/evaluation/benchmark_results.json`
- `docs/evaluation/benchmark_results.csv`
- `docs/evaluation/EVALUATION_REPORT.md`

本次结果表明：混合 RAG 将必答点覆盖率从 76.0% 提升到 95.8%，引用有效率从 0% 提升到 100%；完整多 Agent + Reflexion 同样达到 95.8%，但延迟和调用成本更高。因此答辩时应把多 Agent 的价值表述为职责隔离、可解释、可恢复和引用审计，而不是声称它必然提升准确率。
